import { SalePricePipe } from './sale-price.pipe';

describe('SalePricePipe', () => {
  it('create an instance', () => {
    const pipe = new SalePricePipe();
    expect(pipe).toBeTruthy();
  });
});
